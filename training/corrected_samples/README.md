# Corrected Samples (Training Data)

This folder stores cropped visual pen-strokes (`.png` files named by their Average Hash) generated when users verify and approve handwriting adjustments.

These image samples are used by the visual similarity engine in `backend/audit_service.py` to auto-correct similar handwriting patterns.
